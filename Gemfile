source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"

# Stdlib gems removed from Ruby's default set (Ruby 3.4+/4.x) that
# Jekyll 3.9 (pinned by github-pages) still expects to be present.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
